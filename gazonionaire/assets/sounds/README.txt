Drop short .wav (or .mp3 — but rename the <audio src> in index.html) files here.
Missing files are tolerated; play() failures are silent.

REQUIRED FILES (filename : trigger)

  click.wav    button clicks (splash, wait day)
  buy.wav      buying a commodity
  sell.wav     selling a commodity
  travel.wav   ship leaves a location
  event.wav    neutral random event
  good.wav     positive event / repay loan / borrow
  bad.wav      negative event (pirates, customs)
